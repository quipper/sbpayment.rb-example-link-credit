<%= form_tag(@link_endpoint_url, method: :post, 'accept-charset' => 'Shift_JIS') do |f| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h2>Awesome product</h2>
  <h3>Price: 100 yen</h3>

  <% @req.attributes.each_pair do |name, value| %>
    <%= hidden_field_tag name, value %>
  <% end %>

  <div class="actions">
    <%= submit_tag %>
  </div>
<% end %>
